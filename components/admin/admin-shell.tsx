import { BarChart3,CarFront,FileText,LogOut,MessageSquare,Search,ShieldAlert } from 'lucide-react';import { chatGPTSignInPath,chatGPTSignOutPath,getChatGPTUser } from '@/app/chatgpt-auth';import { isAdmin } from '@/services/admin-auth';import { Logo } from '@/components/nero/logo';
const links=[['Panel','/admin',BarChart3],['Vehículos','/admin/vehiculos',CarFront],['Solicitudes de venta','/admin/solicitudes-venta',FileText],['Búsquedas','/admin/busquedas',Search],['Consultas','/admin/consultas',MessageSquare]] as const;
export async function AdminShell({children}:{children:React.ReactNode}){const user=await getChatGPTUser();if(!user)return <main className="admin-login"><Logo/><h1>Administración NERO</h1><p>Iniciá sesión con la cuenta autorizada para continuar.</p><a className="button button-light" href={chatGPTSignInPath('/admin')} target="_top">Ingresar con ChatGPT</a></main>;let admin=null;try{admin=await isAdmin()}catch{}if(!admin)return <main className="admin-login"><ShieldAlert/><h1>Acceso restringido</h1><p>La cuenta <strong>{user.email}</strong> no figura entre los administradores habilitados.</p><a href="/" className="button button-outline">Volver al sitio</a></main>;return <div className="admin-app"><aside className="admin-sidebar"><a href="/"><Logo compact/></a><nav>{links.map(([label,href,Icon])=><a href={href} key={href}><Icon/>{label}</a>)}</nav><a className="admin-signout" href={chatGPTSignOutPath('/')}><LogOut/>Salir</a></aside><div className="admin-surface"><header><div><span>Panel privado</span><strong>{user.displayName}</strong></div><a href="/" target="_blank">Ver sitio ↗</a></header>{children}</div></div>}
