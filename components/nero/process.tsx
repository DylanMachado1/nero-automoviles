'use client';
import { useState } from 'react';
const flows=[{key:'sell',steps:[['Nos enviás tu vehículo','Nos contás sobre tu auto y compartís su información.'],['Revisamos los detalles','Conocemos el vehículo y acordamos los siguientes pasos.'],['Preparamos la publicación','Presentamos tu auto de forma clara y profesional.'],['Buscamos interesados','Difundimos, atendemos y filtramos las consultas.'],['Te acompañamos','Coordinamos y acompañamos la negociación entre las partes.']]},{key:'buy',steps:[['Nos contás qué buscás','Definimos tus preferencias y tu presupuesto.'],['Analizamos opciones','Buscamos vehículos que se adapten a lo que necesitás.'],['Te presentamos vehículos','Compartimos la información para que puedas decidir.'],['Coordinamos el contacto','Organizamos el siguiente paso con el propietario.'],['Te acompañamos','Seguimos a tu lado durante el proceso.']]}];
export function Process(){const[active,setActive]=useState('sell');const flow=flows.find(({key})=>key===active)??flows[0];return <div className="process-tabs"><div className="process-tab-list" role="tablist" aria-label="Proceso NERO">{[['sell','Quiero vender'],['buy','Quiero comprar']].map(([key,label])=><button key={key} id={`process-tab-${key}`} className="process-tab" type="button" role="tab" aria-selected={active===key} aria-controls={`process-panel-${key}`} tabIndex={active===key?0:-1} data-active={active===key?'':undefined} onClick={()=>setActive(key)} onKeyDown={(event)=>{if(event.key==='ArrowLeft'||event.key==='ArrowRight'){event.preventDefault();const next=key==='sell'?'buy':'sell';setActive(next);document.getElementById(`process-tab-${next}`)?.focus()}}}>{label}</button>)}</div><div id={`process-panel-${flow.key}`} role="tabpanel" aria-labelledby={`process-tab-${flow.key}`}><div className="steps">{flow.steps.map(([title,description],i)=><article className="step" key={title}><span className="step-number">0{i+1}</span><h3>{title}</h3><p>{description}</p></article>)}</div></div></div>}
