'use client';
import { useState } from 'react';import { SlidersHorizontal,X } from 'lucide-react';import { departments,fuelOptions,transmissionOptions } from '@/components/forms/fields';import { sitePath } from '@/lib/brand';
export function CatalogFilters({defaults}:{defaults:Record<string,string|undefined>}){const[open,setOpen]=useState(false);return <><button className="filter-mobile-button" type="button" onClick={()=>setOpen(true)} aria-expanded={open} aria-controls="catalog-filters"><SlidersHorizontal/> Filtros</button><form id="catalog-filters" className={`catalog-filters ${open?'is-open':''}`} method="get"><div className="filter-heading"><div><span>Filtrar por</span><small>Encontrá opciones dentro del catálogo real.</small></div><button type="button" onClick={()=>setOpen(false)} aria-label="Cerrar filtros"><X/></button></div><label>Marca<input name="brand" defaultValue={defaults.brand}/></label><label>Modelo<input name="model" defaultValue={defaults.model}/></label><div className="filter-pair"><label>Año desde<input type="number" name="yearFrom" defaultValue={defaults.yearFrom}/></label><label>Año hasta<input type="number" name="yearTo" defaultValue={defaults.yearTo}/></label></div><div className="filter-pair"><label>Precio mín.<input type="number" name="priceMin" defaultValue={defaults.priceMin}/></label><label>Precio máx.<input type="number" name="priceMax" defaultValue={defaults.priceMax}/></label></div><label>Kilometraje máximo<input type="number" name="mileage" defaultValue={defaults.mileage}/></label><label>Combustible<select name="fuel" defaultValue={defaults.fuel??''}><option value="">Todos</option>{fuelOptions.map(v=><option key={v}>{v}</option>)}</select></label><label>Transmisión<select name="transmission" defaultValue={defaults.transmission??''}><option value="">Todas</option>{transmissionOptions.map(v=><option key={v}>{v}</option>)}</select></label><label>Departamento<select name="department" defaultValue={defaults.department??''}><option value="">Todo Uruguay</option>{departments.map(v=><option key={v}>{v}</option>)}</select></label><button className="button button-light" onClick={()=>setOpen(false)}>Aplicar filtros</button><a href={sitePath('/vehiculos')} className="clear-filters">Limpiar filtros</a></form>{open&&<button className="filter-backdrop" type="button" aria-label="Cerrar filtros" onClick={()=>setOpen(false)}/>}</>}
