import { ArrowUpRight,MapPin } from 'lucide-react';import type { VehicleSummary } from '@/types/nero';import { formatMileage,formatPrice } from '@/lib/format';import { sitePath } from '@/lib/brand';
const imageUrl=(key:string)=>`/api/vehicle-images/${key.split('/').map(encodeURIComponent).join('/')}`;
export function VehicleCard({vehicle}:{vehicle:VehicleSummary}){return <a className="vehicle-card" href={sitePath(`/vehiculos/${vehicle.slug}`)}><div className="vehicle-card-image">{vehicle.imageKey?<img src={imageUrl(vehicle.imageKey)} alt={`${vehicle.brand} ${vehicle.model}`} loading="lazy" decoding="async"/>:<span>Fotografía pendiente</span>}{vehicle.status==='RESERVADO'&&<strong>Reservado</strong>}</div><div className="vehicle-card-body"><span>{vehicle.year} · {formatMileage(vehicle.mileage)}</span><h2>{vehicle.brand} {vehicle.model}</h2>{vehicle.version&&<p>{vehicle.version}</p>}<div><span><MapPin/> {vehicle.city}, {vehicle.department}</span><strong>{formatPrice(vehicle.price,vehicle.currency)}</strong></div></div><ArrowUpRight className="vehicle-card-arrow"/></a>}
